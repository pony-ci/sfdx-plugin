{
  "name": "@pony-ci/sfdx-plugin",
  "version": "1.0.0",
  "author": "Ondřej Kratochvíl",
  "bugs": "https://github.com/pony-ci/sfdx-plugin/issues",
  "dependencies": {
    "@oclif/command": "^1",
    "@oclif/config": "^1",
    "@oclif/errors": "^1",
    "@pony-ci/cli-exec": "^1.0.6",
    "@pony-ci/sfdx-node": "^1.0.4",
    "@salesforce/command": "^2",
    "@salesforce/core": "^2",
    "fs-extra": "^8.1.0",
    "slash": "^3.0.0",
    "tslib": "^1"
  },
  "devDependencies": {
    "@adobe/jsonschema2md": "^4.1.1",
    "@oclif/dev-cli": "^1",
    "@oclif/plugin-help": "^2",
    "@oclif/test": "^1",
    "@salesforce/dev-config": "1.4.1",
    "@types/chai": "^4.2.11",
    "@types/fs-extra": "^8.1.0",
    "@types/mocha": "^7.0.2",
    "@types/node": "^10.17.18",
    "ajv": "^6.12.0",
    "chai": "^4.2.0",
    "cpy-cli": "^3.1.0",
    "globby": "^8",
    "json-schema-to-typescript": "^8.2.0",
    "lodash": "^4.17.15",
    "mocha": "^7.1.1",
    "nyc": "^14.1.1",
    "ts-node": "^8",
    "tslint": "^6.1.1"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "files": [
    "/lib",
    "/types",
    "/messages",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "homepage": "https://github.com/pony-ci/sfdx-plugin",
  "keywords": [
    "sfdx-plugin",
    "salesforcedx",
    "sfdx",
    "salesforce",
    "pony",
    "pony-ci",
    "ponyci"
  ],
  "license": "MIT",
  "oclif": {
    "commands": "./lib/commands",
    "bin": "sfdx",
    "devPlugins": [
      "@oclif/plugin-help"
    ]
  },
  "scripts": {
    "postpack": "rm -f oclif.manifest.json",
    "posttest": "tslint -p test -t stylish",
    "prepack": "rm -rf lib && tsc -b  && oclif-dev manifest && oclif-dev readme && npm run schema2ts",
    "docs": "rm -r docs/reference/config; mkdir -p docs/reference/config; jsonschema2md -o docs/reference/config -d src/schema -x -",
    "schema2ts": "(cd src/schema && json2ts -i . -o ../../types --bannerComment \"/* tslint:disable */\")",
    "test": "nyc --extension .ts mocha --forbid-only \"test/**/*.test.ts\"",
    "version": "oclif-dev readme && git add README.md",
    "watch": "rm -rf lib && tsc --watch"
  }
}
